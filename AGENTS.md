# 微信视频号多账号内容运营 Monorepo 规则

## CURRENT_ACCOUNT

处理任何账号内容任务前，必须先确定 `CURRENT_ACCOUNT`。允许值只有：

| CURRENT_ACCOUNT | 账号 | 常用别名 |
| --- | --- | --- |
| `gzminge` | 广州敏哥聊财税 | 敏哥、广州敏哥 |
| `gzxzcs` | 广州小张说财税 | 小张、广州小张 |
| `qycslc` | 备用 | 备用账号 |
| `gzcktxpp` | 广州出口退税-翩翩 | 翩翩、出口退税翩翩 |
| `tsxbj` | 退税小笔记 | 小笔记、出口退税小笔记 |
| `gzlxcs` | 广州老徐聊企业财税合规 | 老徐、广州老徐 |

- 用户明确给出账号 ID、账号名或唯一别名时，将其设为本次连续任务的 `CURRENT_ACCOUNT`。
- 无法唯一识别时，先询问账号，不得猜测。
- 用户明确切换账号时，立即丢弃上一账号的内容快照，再加载新账号上下文。
- 跨账号对比、迁移、索引维护或系统维护任务可读取多个账号，但必须是用户明确要求或完成维护所必需。

## Account Context Lock

确定 `CURRENT_ACCOUNT=<id>` 后，本次任务默认只允许读取：

```text
.codex/**
shared/**
accounts/<id>/account.yaml
accounts/<id>/内容库/**
```

默认禁止读取其他 `accounts/*`。写入也只能发生在当前账号目录；不得把历史、灵感、候选、复盘、内容地图、定位或素材跨账号合并。

## Context Loading 与阶段交接

读取仓库不等于广泛加载仓库。根 `AGENTS.md` 只提供账号、路由、权限、事实边界和阶段边界；确定当前阶段后，按以下顺序读取：

```text
AGENTS.md
→ CURRENT_ACCOUNT
→ 当前阶段 Skill
→ 该 Skill 明确要求的 shared 规则
→ 当前账号最少必要文件
→ 用户明确引用的当前任务来源
```

- 每个生产阶段使用独立对话，并按独立内容继续隔离：一条独立内容对应一个 `topic_id`、一个 `content_id`、一份 Handoff 和一个下游文案对话。新对话不继承上一阶段的聊天记录、检索过程、淘汰方案、未采用草稿或无关历史。
- 选题进入文案的新对话明确引用 Handoff 文件时，以该文件作为选题阶段唯一工作成果；不得再从仓库历史、旧 Skill、索引、推荐记录、聊天记忆或草稿重建上一阶段。
- 文案阶段必须把 Handoff 中的选题来源、用户 `selected` 反馈和未完成 `pending_repo_actions` 原样继承到最终 Repo 内容文档；这些信息只用于后续 Repo 补写，不参与文案创作。直接给题或直接参考重写且没有项目推荐时明确标记为“不适用”，不得虚构反馈。
- 视觉规划的新对话明确引用 `视觉规划输入` 文档时，以该文件中的最终标题、最终口播和确认事实作为上一阶段唯一工作成果；不得用 Handoff、Repo 内容文档或聊天记忆重建文案阶段。
- 当前阶段只读取当前 Skill 的 Required Context。上一阶段 Skill、下游规则、推荐历史、候选或灵感索引、历史正文和派生资产，除非当前 Skill 明确要求，否则不得加载。
- 仓库规则负责流程、权限、账号、事实来源和专业边界；Handoff 只负责“已采用选题 → 对应文案”的阶段交接。文案确认后的 Repo 执行输入与口播视觉输入按 `shared/schemas/confirmed-copy-delivery-schema.md` 生成，不再使用 Handoff。
- Handoff 和视觉规划输入都是 ChatGPT 项目中的临时共享文件，不是仓库事实源，不写入 Repo，不更新任何 Index、Cache、状态或历史。Repo 内容文档是供 Codex 执行的确认内容来源，本身也不证明已经写入、发布或获得归档授权。

选题 Handoff 的字段、有效性和命名统一引用 `shared/schemas/handoff-packet-schema.md`；文案确认后的交付文件统一引用 `shared/schemas/confirmed-copy-delivery-schema.md`。选题被用户正式采用后按 `shared/schemas/content-identity-schema.md` 建立稳定 `content_id`；后续文案、视觉、发布和归档继续使用同一 ID，不得按阶段重新生成。

阶段结束时：

- 一个选题被采用并明确进入文案阶段：当前选题对话只生成对应的一份 Handoff，提示用户在项目中新建一个文案对话并引用，然后停止；不得在当前对话加载或执行文案 Skill。
- 同一轮采用多个彼此独立的选题：各自建立不同的 `content_id` 与 `selected` 反馈事件，各自生成一份 Handoff，并逐项提示用户分别新建文案对话；它们只共享原 `recommendation_batch_id`，不得共用内容身份、Handoff 或下游对话。全部 Handoff 生成后结束当前选题阶段。
- 用户要求把多个方向合并成一篇时，先在当前选题阶段确认合并后的唯一选题，再按单一内容生成一份 Handoff；“有意向”“先留着”等未明确进入生产的表达不生成确认态 Handoff。无法判断是分别制作还是合并时，先询问用户。
- 短文正文与唯一最终标题确认后：生成一份 Repo 内容文档并结束，不生成 Handoff，不自动发布或归档。
- 口播正文确认后：必须检查用户是否从三个主标题方案中明确选定最终标题；未选定时主动提醒，正文只标记为“已确认，标题待确认”，不得生成最终文件。
- 口播正文与唯一最终标题均确认后：同时生成 Repo 内容文档和视觉规划输入文档并结束。即使用户同时要求规划视觉，也只提示其新建视觉对话并 `@视觉规划输入` 文档，不得在当前对话加载或执行视觉 Skill。
- 用户尚未确认当前阶段所需的全部最终信息：继续当前阶段确认，不生成标记为 `confirmed_by_user` 的最终文件。

在 ChatGPT“对话”模式的项目中，通过 GitHub 集成读取仓库时，一律把该集成视为只读：不得调用仓库写入、创建文件、提交或 push 能力，不得发起写入授权请求，也不得先尝试写入再根据 `403` 回退。选题推荐与用户反馈先在当前对话形成稳定事件；用户采用选题并生成 Handoff 时，尚未执行的完整事件直接写入 `pending_repo_actions`。此时只说明“已登记为待 Codex 执行”，不得显示为写入失败。

只有当前运行环境直接具备可写的本地 Repo 或工作树，并且能执行所需脚本时，才视为具备仓库写入能力。其他没有仓库写入或脚本执行能力的环境只完成允许的判断和文件交付；文案阶段允许但未执行的动作写入 Repo 内容文档。不得声称已经保存、更新索引、校验或同步。具有写入能力时，仍只执行当前 Skill 明确允许的写入。

## Skill Router 与阶段边界

| 用户当前明确请求 | Skill | 完成边界 |
| --- | --- | --- |
| 保存参考内容、竞品内容、图片文字、客户反馈或想法 | `idea-intake` | 录入一条灵感并更新 Idea Index 后结束 |
| 给选题、分析内容缺口、分析灵感或判断是否入池 | `topic-planning` | 完成分析或默认给出 5 个合格选题；可写环境保存最小推荐记录，Chat 对话只保留待执行事件；不足不凑数 |
| 已确认选题，明确要求短文字幕、文字播报、打字字幕或短文 | `text-broadcast-copywriting` | 交付短文；最终正文与标题确认后生成 Repo 内容文档并结束 |
| 已确认选题，明确要求真人口播、口播稿或出镜讲解 | `spoken-copywriting` | 交付口播；最终正文与标题确认后生成 Repo 内容文档和视觉规划输入文档并结束 |
| 已有明确引用的视觉规划输入文档或用户提供的最终口播音频及唯一最终标题，并明确要求 PPT 图片、PPT 页面视觉、口播 PPT 配图、PPT 生图 Prompt、PPT执行指南、配图、示意图、中间画面、视觉分镜或 AI 生图提示词 | `spoken-visual-planning` | 先交付 3:4 搜索封面、16:9 推荐流/PPT封面与可独立阅读的 PPT 分页内容并等待确认；确认后完成逐页视觉执行方案、所需生图 Prompt 与整体 QA，交付 PPT设计执行指南和剪辑分段表后结束 |
| 已确认选题，仅要求“写文案”但未指定载体 | 按 `CONTENT_FORMAT` 路由 | 交付对应载体文案后结束 |
| 内容已实际发布且明确要求归档/写库 | `publish-archive` | 写历史、增量更新索引和候选状态后结束 |

每次只完成用户当前明确要求的阶段。灵感录入不自动选题，选题不自动写文案，文案完成或用户采用不等于发布；仅说“已发布”但没有明确归档指令时也不得写库。

`spoken-copywriting` 完成后不得自动进入 `spoken-visual-planning`。只有用户明确提出 PPT 图片、PPT 页面视觉、口播 PPT 配图、PPT 生图 Prompt、PPT执行指南、口播视觉辅助、示意图、配图或 AI 生图提示词需求时才路由到该 Skill。`spoken-visual-planning` 完成后不得自动生图、归档或进入其他生产阶段。

### CONTENT_FORMAT 路由

`CONTENT_FORMAT` 只表示当前正式内容载体，允许值为 `text_broadcast`、`spoken`，不得加入内容状态机。

`spoken-visual-planning` 是 `spoken` 内容完成后的可选生产阶段，不属于 `CONTENT_FORMAT` 枚举。

判断优先级：用户明确指定 > 已确认候选的 `recommended_format` > 旧流程默认 `text_broadcast`。

- 用户明确要求口播、真人口播、出镜讲或口播稿：`spoken`。
- 用户明确要求字幕短文、文字播报、打字字幕或短文：`text_broadcast`。
- 用户未指定时，候选推荐 `spoken` 或 `text_broadcast` 则采用对应载体。
- 候选推荐 `either`、旧候选缺少推荐字段或用户直接给题时，默认 `text_broadcast`。
- `recommended_format` 只是建议，永远不得覆盖用户明确选择。

## 规则权威与事实边界

- 本文件是账号锁、Skill 路由、`CONTENT_FORMAT` 和全局阶段边界的唯一项目级来源。
- `shared/` 是公共规则、Schema、状态机、索引规则和维护脚本的唯一公共来源。创作角色、媒介方法、页面职责和阶段内验收只在对应 Skill 及其专属 reference 中定义，不在本文件重复。
- `shared/rules/acquisition-and-fact-framing.md` 是选题、文案、视觉封面和 PPT资料内容共同使用的获客表达、信息身份与核验边界来源。所有内容阶段先识别确定性事实、讨论型话题、假设性场景和专业结论，再决定核验要求与最大表达强度。
- 内容生产在法律、平台、业务和不虚构确定性结论的边界内优先最大化目标客户命中、停留、信任和咨询意愿。讨论型话题可以保留数字、结果冲突和风险感；专业结论必须符合当前有效规则并保留改变结论的关键条件。
- 当前账号的业务范围、目标受众与角色表达分别只以该账号 `账号基本定位.md` 和 `账号人设与文风.md` 为来源；不得从其他账号补全或推断。`账号视觉风格.md` 只记录已经明确存在的品牌资产、视觉禁用项和角色视觉理解上下文，不得预设固定配色、背景、渲染、材质、版式或行业元素。
- 灵感、候选与发布状态只以 `shared/schemas/content-state-machine.md` 为定义来源；正式历史 Markdown 是已发布事实源，Index 是可重建检索层，内容地图与复盘是派生资产。
- 作为确定性事实或专业结论使用的政策、税率、期限、处罚标准、平台规则、地方规则和办理条件，必须核验当前有效的官方来源。为解释业务场景可以构造明确标识的假设或合成情境，但不得把生成内容冒充真实客户经历、公共讨论、已核验案例、处罚或办理结果，也不得承诺办理、退税或合规结果。

## 写入与同步边界

- Repo 始终只保存一套长期正式事实，不按生产阶段保存多份 Handoff 或视觉规划输入。聊天中的中间修改留在当前对话，只有规则明确允许且具备写入能力的结果才进入 Repo。
- Handoff、视觉规划输入、视觉规划、双封面方案、PPT 分页内容、AI 生图提示词、PPT 设计执行指南和剪辑分段表保留在 ChatGPT 项目中，不属于仓库同步范围。完成视觉规划不得触发仓库写入或 GitHub 同步。
- 用户确认的最终文案不因“确认”自动写入 Repo；只有用户明确要求保存或同步，且存在对应写入规则时才执行。实际发布内容仍只通过 `publish-archive` 进入历史事实源。
- 项目规则不规定用户选择 Chat、Work、Codex、具体模型、推理强度或个人资源安排。项目只保证阶段边界、最小上下文、交接格式和写入一致性。

- 推荐记录的分层读取及派生维护以 `shared/rules/topic-memory-reading.md` 为准。允许普通选题和明确反馈记录后增量维护本账号推荐检索缓存与偏好摘要；这不授权重建内容地图或其他复盘。原始记录保留，旧题通过按需检索查重，明确否定不随时间自动失效。

- 普通选题允许自动保存当前账号的最小推荐记录及明确的选题反馈，按 `shared/schemas/topic-recommendation-log-schema.md` 执行。该记录独立于候选与灵感状态，不自动建候选卡、入池或改变内容状态；用户明确要求不记录时服从用户。发布效果仅在用户明确要求记录或复盘时保存。
- 连续任务中用户明确选择、跳过或否定已推荐题时，应按上述规则记录原话及作用范围，包括选择后直接进入文案的情况；完成最小反馈记录后继续用户所要求阶段，不为记录而重新选题。
- 选题发生在只读 Chat 且推荐或反馈尚未写入时，Handoff 必须携带可幂等补写的完整事件；文案完成后由 Repo 内容文档继续承载，直到 Codex 核验并执行或确认已经存在。不得在阶段转换中丢弃、概括或重建事件 ID。

- 写入前确认目标仍属于 `CURRENT_ACCOUNT`，不得覆盖同名或疑似重复记录。
- 索引更新应优先增量 append/update；全量 rebuild 仅用于迁移、修复或明确维护。
- 除非用户明确要求完整覆盖分析、复盘或派生资产重建，归档后不得自动重建全部内容地图。
- 用户明确要求 GitHub 同步、提交或 push 时，先读取 `GitHub-Sync-Rules.md`。
